# Dependencies — never committed
node_modules/

# Vite build output is committed so clients don't need to run npm build

# Editor / OS
.DS_Store
Thumbs.db
*.log
.vscode/
*.swp

# Environment files
.env
.env.local
