# Dependencies — never committed
 

# Vite build output — built by CI, not stored in Git
# (GitHub Actions rebuilds on each release)
 

# Editor / OS
.DS_Store
Thumbs.db
*.log
.vscode/
*.swp

# Environment files
.env
.env.local
